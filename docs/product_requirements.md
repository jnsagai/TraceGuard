# Product Requirements

TraceGuard verifies derivation coverage between higher-level and lower-level natural-language requirements using a deterministic, auditable core.

AI-generated artifacts may be imported as proposals, but AI is advisory only. Deterministic rules produce the final coverage and validation decisions.

